// export interface Pagination {
//   limit: number;       // Cantidad de registros por página
//   current: number;     // Número de página actual
//   items: number;       // Cantidad total de registros
//   pages: number;       // Cantidad total de páginas
//   next: number | null; // Página siguiente (o null si no existe)
//   prev: number | null; // Página anterior (o null si no existe)
// }
